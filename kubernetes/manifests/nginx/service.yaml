apiVersion: v1
kind: Service

metadata:
  name: nginx-projeto-korp
  namespace: projeto-korp

spec:
  type: NodePort

  selector:
    app: nginx-projeto-korp

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080